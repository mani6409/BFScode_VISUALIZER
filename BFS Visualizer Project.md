# BFS Visualizer Project

This project demonstrates **Breadth-First Search (BFS)** with both:
- A **Python implementation** (backend)
- An **HTML/JS visualizer** (frontend with animation)

---

## 📂 Files
- `bfs.py` → BFS algorithm in Python (saves results into `bfs_output.json`)
- `bfs_visualizer.html` → Visualizes BFS traversal with animation
- `bfs_output.json` → Example BFS output
- `README.md` → Instructions

---

## 🚀 Usage
### Step 1: Run BFS in Python
```bash
python bfs.py
